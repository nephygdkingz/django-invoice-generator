DATABASE_URL=postgres://invoice_user:password@localhost:5432/invoice_local
DEBUG=True
SECRET_KEY="secretkeyhere"
ALLOWED_HOSTS=localhost,127.0.0.1

if not request.user.userprofile.is_paid:
    # Remove logo, add watermark, limit features
    invoice_info['company_logo'] = None
    # Or redirect to upgrade page